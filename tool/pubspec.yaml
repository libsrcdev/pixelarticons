name: pixelarticons_tool
description: "CLI tool to sync, download, and publish pixelarticons Flutter package"
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  http: ^1.2.0
  yaml: ^3.1.2
  archive: ^4.0.2
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lint: ^2.3.0
